%h1.title= @theatre.title

%table.show-table
  %thead
    %th Theatre
    %th Movies
    %th Marathon count
  %tbody
    %tr
      %td= @theatre.title
      %td= @theatre.movies.uniq.map(&:title).join(", ")
      %td= @theatre.marathons.count
